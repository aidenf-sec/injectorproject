# Virtual environment
venv/
.venv/

# API keys — NEVER commit these
.env

# Python
__pycache__/
*.pyc
*.pyo

# Test results — all output goes to reports/ by default.
# Red team transcripts can contain system prompts, attack payloads, and raw
# model responses. Never commit them to a public repo without reviewing first.
# If you want to share example output, copy a sanitized version elsewhere.
reports/

# IDE
.vscode/
.idea/
